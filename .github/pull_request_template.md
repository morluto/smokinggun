## Summary

<!-- What does this change do, and why? -->

## Testing

<!-- What did you run to validate this change? Include the relevant commands and results. -->

- [ ] `pnpm typecheck`
- [ ] `pnpm test`
- [ ] `pnpm lint` and `pnpm format:check`
- [ ] `pnpm knip` and `pnpm check:boundaries`
- [ ] `pnpm test:coverage` (if coverage-relevant)
- [ ] `pnpm build` (+ `pnpm evaluate:corpus` for scanner changes, `pnpm test:cli` for CLI changes)

## Context

<!-- Link to issues, related PRs, or the authority architecture when relevant. -->

## Release

<!-- Did this change user-visible behavior? If so, run `pnpm changeset` so the release
workflow can bump the version and generate release notes. -->
