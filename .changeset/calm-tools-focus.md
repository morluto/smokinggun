---
"smokinggun": minor
---

Default repository scans to runtime source while preserving auxiliary files in inventory, with `--source-profile all` and explicit `--only` paths available to scan tests, documentation, examples, and fixtures.
